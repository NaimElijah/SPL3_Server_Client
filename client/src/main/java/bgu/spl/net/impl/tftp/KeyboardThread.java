package bgu.spl.net.impl.tftp;

public class KeyboardThread implements Runnable{

    private sometype input_place;

    public KeyboardThread(sometype input_place){
        this.input_place = input_place;  //  so it will add to that input_place
    }

    @Override
    public void run() {
        // TODO: add the while loop that constantly gets unput from the user
        while (input = line.innnnn != null){
            // do something with the input
        }
    }
    
}
